<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KanjiMori" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/button_background_night</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/button_text_night</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/button_background_night</item>
        <item name="colorSecondaryVariant">@color/leaf_green</item>
        <item name="colorOnSecondary">@color/button_text_night</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/app_background_night</item>
        <item name="android:textColor">@color/text_color_night</item>
    </style>
</resources>
